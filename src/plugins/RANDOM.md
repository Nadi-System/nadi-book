# Env Functions
## random_bool {#env.random_bool}
```sig
env RANDOM.random_bool(prob: 'f64' = 0.5, seed: 'Option < u64 >')
```

**Arguments:**
- `prob: 'f64' = 0.5` => 
- `seed: 'Option < u64 >'` => 

Random bool given uniform probability
